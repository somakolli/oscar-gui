import { ItemDetailPipe } from './item-detail.pipe';

describe('ItemDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
