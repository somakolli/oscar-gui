import { HumanReadableNumbersPipePipe } from './human-readable-numbers-pipe.pipe';

describe('HumanReadableNumbersPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HumanReadableNumbersPipePipe();
    expect(pipe).toBeTruthy();
  });
});
